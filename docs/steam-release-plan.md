# 变量城夜巡 Steam 化推进计划

目标：保留 GitHub Pages 在线试玩，同时逐步形成可提交 Steam Demo / 正式版的桌面发行能力。

## 当前原则

- `index.html` 和 `src/` 继续作为网页试玩入口，GitHub Pages 必须始终可玩。
- Steam 专属能力通过平台适配层接入，网页环境提供 no-op 或 localStorage fallback。
- 每轮自动推进至少交付一个端到端切片，并完成语法、资源、页面或构建验证。
- 所有涉及图片素材的任务，包括地图元素、怪物、武器、特效、UI、Steam 商店图、截图占位和宣传素材，都优先自行生成、导出或处理成好看的可用素材，并保存到仓库合适目录；不要只写素材需求清单。
- 必须持续推进局外增强设定和实现，包括长期资源、档案级解锁、章节推进奖励、武器/装备/角色成长节点、清晰数值上限、重开动力和存档兼容。
- 新依赖、桌面构建、Steamworks SDK 接入需要按权限流程请求批准。

## 优先级路线

1. 平台适配层
   - 抽象存档、设置、全屏、成就、手柄状态。
   - 网页版仍使用 localStorage，桌面版改为文件存档并预留 Steam Cloud。
2. 基础 PC / Steam Deck 体验
   - 暂停菜单、设置菜单、全屏切换。
   - 手柄移动、修复脉冲、闪避、暂停。
   - UI 提示区显示当前输入方式。
3. 桌面发行壳
   - 添加 Electron 构建目录。
   - 输出 Windows 可执行目录，保持静态网页入口复用。
   - 后续接 SteamPipe 上传目录。
4. 文件存档与 Steam Cloud
   - 将跑局、档案、设置封装为平台存储 API。
   - 桌面版落地 JSON 文件，预留 Steam Auto-Cloud 路径。
5. 音频系统
   - BGM、环境氛围、Boss 音乐、危险预警、UI 音效。
   - 音量设置和静音能力。
6. 成就与商店素材
   - 先做本地成就事件，再接 Steamworks。
   - 准备 capsule、截图、宣传片素材清单。
7. 稳定性与性能
   - 30 分钟运行压测。
   - 全章节通关路线检查。
   - Canvas 非空、资源缺失、控制台错误自动检查。

## 当前切片

- [x] 五章内容、地图、Boss 机制已经形成可试玩主线。
- [x] 添加平台适配层，保证 Pages 和桌面版共用核心逻辑。
- [x] 添加暂停、设置、全屏、手柄基础输入。
- [x] 为桌面壳接入准备不破坏网页的目录结构。
- [x] 增加 Electron 桌面壳预检和可自动退出的桌面窗口 smoke test。
- [x] 将桌面存档路径整理成 Steam Auto-Cloud 配置草案。
- [x] 安装 Electron 依赖并运行 `npm run desktop:smoke` 验证桌面窗口启动。
- [x] 解决 Electron 二进制下载超时，生成 `package-lock.json`，详见 `docs/steam-desktop-smoke.md`。
- [x] 接入音频系统的设置项、静音和第一批 UI / 战斗音效。
- [x] 增加本地成就事件、成就清单与 Steamworks 映射草案。
- [x] 准备 Steam 商店截图、capsule 和宣传片素材清单。
- [x] 从 capsule source board 导出最终 capsule PNG，并接入尺寸校验流程。
- [x] 接入截图自动捕获流程，生成首批 1920x1080 商店截图并做尺寸校验。
- [x] 人工复核首批商店截图并按 Steam 页面顺序筛选最终展示图。
- [x] 补齐 Steam 商店页面文案、标签、支持语言与最低配置草案。
- [x] 生成 Steam 商店页面文案预览图，便于人工检查中英文排版和卖点顺序。
- [x] 制作首版 Steam 新闻/公告草案，用于 Demo 上线前说明玩法范围、路线图和反馈入口。
- [x] 针对公告草案生成 Steam 富文本粘贴版，减少发布前的手工排版成本。
- [x] 生成 Steam 活动本地化导入 CSV 草案，方便后续批量录入事件标题、摘要和正文。
- [x] 回到 Electron 阻塞项，清理/重装 Electron 二进制并重新尝试 `npm run desktop:smoke`。
- [x] 运行 `npm run dist:win` 并校验 `dist/steam-demo` 目录包可作为 SteamPipe 上传源。
- [x] 稳定 Electron Builder 在自动化中的退出行为，避免目录包已产出但命令超时。
- [x] 增加桌面版长时间运行巡检脚本，采样 10-15 分钟内的页面错误、FPS 区间、存档写入和内存占用。
- [x] 扩展桌面巡检为章节路线压力测试，覆盖全章节入口、Boss 触发、存档恢复和长地图移动。
- [x] 针对章节路线压力测试暴露的地图尺寸差异，继续把第二至五章地图扩展为更大的分区式地图。
- [x] 强化章节路线压力测试的存档探针，确认读回的是本章本次写入的跑局档。
- [ ] 为第二至五章新增更多章节专属地图装饰素材和商店截图候选构图，继续提升 Steam 页面第一眼质感。
- [x] 设计并实现局外增强第一版：长期资源、档案级解锁、重开奖励反馈和与现有本地/Steam Cloud 存档兼容的数据结构。
- [x] 扩展局外增强第二版：加入更多章节条件节点、武器专精解锁、局外成长音效和更清晰的 Steam Deck 可读反馈。
- [x] 为局外增强第三版补充专精视觉素材、节点解锁动效和更明确的武器专精构筑路线展示。
- [x] 回到第二至五章章节专属地图装饰素材，补充地图视觉徽章、商店截图候选构图和对应资源尺寸校验。
- [x] 增加“夜巡委托”爆品化钩子：开始菜单展示本轮推荐挑战，跑局内追踪限时目标，完成后即时给校准碎片/补给并写入档案。
- [x] 增加战斗节奏连段：连续击破会点燃 HUD 节奏条、给即时补给并记录档案最佳连段，强化首局 30 秒爽感。
- [x] 接入地图探索标记层：从章节 stepTargets/zones/start/bossSpawn 自动绘制任务点、危险区提示、起点和 Boss 地标，并纳入路线压测。
- [x] 增加章节探索回声：五章地图各有可发现剧情回声点，靠近即可收集并获得 bug 点数/经验奖励，收集状态写入跑局存档并纳入路线压测。
- [x] 升级回声为局外收集档案：开始界面展示五章回声进度，回声发现写入长期档案，每章集齐后给一次性校准碎片并纳入自动化探针。
- [x] 增加推荐流派一键开局：开始界面展示三套 30 秒爽点构筑，点击后自动装备推荐武器并直接进入本章开场，纳入自动化探针。
- [x] 为推荐流派补上启动目标：开局短时间内按流派完成击破目标会点亮 HUD、给即时补给并写入跑局存档，强化第一波成型反馈。
- [x] 让推荐流派启动奖励真实改变武器手感：启动目标完成后应用一次性武器超频，并在 HUD、构筑摘要和自动化探针中验证。
- [x] 增加首局开场牵引链：用处理异常、击破首波、点亮构筑三段 HUD 目标串起前 30 秒，并纳入存档和路线压测。
- [x] 增加结算复盘与推荐重开：失败/通关后展示亮点、短板和下一把推荐流派，并写入档案与路线压测。
- [x] 增加五章地图地标补给层：每章新增一个可发现章节装置，靠近自动触发即时奖励、绘制 Canvas 标记并纳入路线压测。
- [x] 升级地标补给为局外地标档案：首次发现写入长期档案，每章归档给校准碎片，并在开始界面和路线压测中验证。
- [x] 增加开场快递裂隙：第一章开局在视野附近生成一组低血量追击怪，HUD 限时追踪清场奖励，并纳入首局 30 秒自动探针。
- [x] 为第二至五章补充可互动地图装置：准点闸门、盐值灯串、叶灯中继、差异控制台靠近触发一次性战斗收益，并纳入路线压测。
- [x] 将互动地图装置升级为局外装置档案：首次启动写入长期档案、章节归档给校准碎片，并在开始界面、结算复盘和路线压测中验证。
- [ ] 继续深化第二至五章地图实装素材：补更多章节专属可互动装饰、近景地标和商店截图候选构图。
- [ ] 继续压测首局 30 秒：观察委托文案、武器选择、第一异常节点、第一波敌人和连段补给是否能稳定形成“再来一把”的爽点。

## 每小时自动检查模板

每次自动执行时：

1. `git status -sb` 和 `git fetch origin`。
2. 阅读本文档，更新“当前切片”状态。
3. 完成一个小切片；每轮优先判断当前最影响吸引力、传播和留存的一环，避免只堆功能。
   - 重点观察前 30 秒钩子、战斗爽感、章节记忆点、局外成长、构筑成型感、剧情悬念和截图卖点。
   - 如果涉及图片素材，先生成或导出素材，再接入代码、数据或文档。
   - 遇到局外增强相关任务时，必须落到真实可玩的代码、数据、UI 和存档迁移检查，不只写设定。
4. 运行：
   - `node --check src/main.js`
   - `node --check src/data/game-data.js`
   - `node --check src/platform/web-platform.js`
   - 新增桌面脚本的 `node --check`
   - 资源引用检查、图片尺寸或透明度等素材检查
   - `npm run desktop:monitor:quick`
   - `npm run desktop:route:quick`
   - 涉及桌面发行时运行 `npm run dist:win`，它会自动串联目录包结构检查和打包 exe smoke test
   - 必要时浏览器本地加载检查
5. 提交并推送 `origin/main`。
6. 检查 GitHub Pages 仍可访问。
## 2026-07-07 自动推进记录

- [x] 为第二至五章重新导出章节地图商店截图候选构图板：每章包含标题、截图焦点、互动装置、危险点/路线 callout 和右侧卖点栏，提升 Steam 页面第一眼可读性。
- [x] 将截图候选字段加入 `chapter-map-asset-manifest.cjs`，并在 `check-chapter-map-assets.cjs` 中校验标题、焦点、装置、标签和 callout，防止后续素材缺关键卖点。
- [x] 加入第二至五章互动装置扫描：HUD 显示最近未启动装置、距离和奖励，Canvas 绘制脉冲导航线/边缘箭头/迷你地图标记，并纳入路线压测。
- [x] 增加失败复盘助推：结算页会根据失败原因生成“护盾起步/连段预热/装置路线补给”等下一局加成，按推荐再来会真实带入开局资源和首个开场目标额外奖励，并纳入路线压测。
- [x] 强化首波快递裂隙动量：裂隙怪加入醒目脉冲标记，清场后补一格战斗节奏并给推荐流派启动信用，让第一波稳定点亮连段补给和武器超频。
- [x] 补强武器选择前置钩子和第一异常节点反馈：三把武器选择卡展示首 30 秒打法承诺，第一异常节点在 Canvas 与目标罗盘中标为“首个异常”，并纳入路线压测。
- [x] 补强第一异常事件选择反馈和武器命中手感：首个异常事件选项展示开场链推进预览，处理后弹出“开场链 +1”，武器/脉冲/盟友命中会显示浮动伤害字，并纳入路线压测。
- [x] 增加失败前公平提示：生命跌入低生命/临界生命时 HUD 变色脉冲、Canvas 弹出危险文字并给短暂无敌窗口，路线压测覆盖低血和临界血两段提示。
- [x] 强化首局武器命中音画节奏：回形针、键盘宏、修正液分别使用断点火花、键帽爆点和雾化扩散，配套独立命中音效并纳入路线压测。
- [x] 补强失败复盘到推荐重开的留存闭环：结算页生成“下一局路线”卡，推荐再来会真实带入 `retryPlan`、缩短第一波裂隙等待并给流派启动信用，开始界面和路线压测同步验证。
- [x] 补强第一波截图卖点：开场快递裂隙生成/清场会写入 `openingShowcase`，HUD 优先展示“第一波卖点”，Canvas 绘制固定卖点卡和取景框，路线压测验证生成态、清场态和推荐重开 30 秒可读性。
- [x] 补强推荐重开 30 秒截图路线：`retryPlan` 应用后立即激活 retry 阶段 `openingShowcase`，HUD 显示“推荐再来”，Canvas 绘制 `RETRY ROUTE` 卡并呈现路线、截图焦点和奖励；结算页“下一局路线”补入截图焦点，自动探针验证推荐再来后的 HUD/状态/复盘文案。
- [x] 稳定桌面路线快检 FPS 判定：Electron 监控报告保留 `rawAverageFps`，但阈值改用跳过启动预热样本后的 steady `averageFps`，避免功能路线全绿时被启动低帧误判失败。
- [x] 补强推荐重开后的实际战斗镜头：`openingShowcase` 新增 combat 阶段，首波裂隙生成后切到“实战镜头”，HUD/Canvas 显示目标罗盘、首波敌群、武器特效、奖励反馈四项清单；武器命中会写入特效状态，路线压测新增 `retryCombatShowcase` 验证“镜头已就绪”。
- [x] 补强失败前后公平反馈闭环：伤害会记录最近伤害/最后一击，低血和临界生命会写入预警与短暂无敌次数；结算页新增“失败回放”卡，显示最后记录、生命变化、反制建议，并把回放类失败接到护盾起步和推荐重开路线，路线压测新增 `failureReplay` 验证。
- [x] 补强第二至五章实战截图记忆点：新增章节实战镜头检查点，每章验证地图装置、章节怪物机制、Boss 弱点窗口和武器特效四项同屏卖点，并纳入路线压测。
- [x] 将第二至五章章节实战镜头转成可复用商店截图素材：新增 `chapter-combat-showcase-candidates-v1.png`，每章展示装置、敌人机制、Boss 窗口和武器特效四项卖点，并纳入商店素材 manifest 与复核校验。
- [x] 将章节实战候选图拆成可排序的单张商店截图：新增第二至五章 4 张 1920x1080 ready 截图，更新 Steam 截图顺序为 10 张，并重建 contact sheet 复核图。
- [x] 将新增章节实战截图从生成候选进一步接入真实 `storeShot=chapter-X-combat` 捕获路由：第二至五章会从可玩版本直接搭建装置、章节敌人机制、Boss 弱点窗口和武器特效，并由 `runChapterCombatStoreShotProbe` 纳入路线压测；已重新捕获 10 张 1920x1080 商店截图。
- [x] 升级商店截图捕获脚本：新增 Node/CDP 捕获器，等待 `window.__variableCityStoreShotReady` 后截图，并支持 `-OnlyId` 按截图 ID 局部重捕；`npm run check` 已纳入新捕获器语法检查。
- [x] 下一轮继续提升 Steam 页面第一眼说服力：复核四张章节实战图在 contact sheet 中的首屏卖点密度，并继续压测首局 30 秒钩子是否稳定形成“再来一把”的爽点。
- [x] 新增 Steam 首屏吸引力自动审计：确认首屏截图固定为 Boss 战斗 + 第二至五章实战证明，校验截图尺寸、文件密度、章节 `storeShot` 路由、`mustShow` 卖点和 contact sheet / 章节实战复核图。
- [x] 新增首局 30 秒开场评级：首个异常、快递裂隙清场、武器超频和推荐重开路线会合成 A/S 级反馈，写入结算、开始界面档案最佳记录，并纳入桌面路线压测。
- [x] 提升首局 30 秒评级自然可达性：普通武器选择也会自动绑定对应推荐流派启动目标，HUD 增加“追S”下一步教练，路线压测覆盖武器选择入口和评级提示。
- [x] 增加首局 30 秒评级时间分布探针：从普通武器选择入口模拟 60fps、30fps、低帧三种节奏，要求三把武器都能在 30 秒内通过首个异常、裂隙清场和武器超频拿到 S 级。
- [x] 增加首局 30 秒路线可读性预告：首个异常完成后在 HUD 和 Canvas 黄框中提前标出下一波快递裂隙位置，并由路线压测验证“裂隙预告/追S/黄框”提示和刷怪承接。
- [x] 强化首局裂隙预告反应窗口：黄框预告延长到 1.15 秒，HUD 显示“4个落点/倒计时”，Canvas 绘制 4 个真实刷怪落点，并由路线压测验证落点在地图内且低帧样本仍能读到。
- [x] 增加首波接怪窗口：快递裂隙怪落地后有 0.55 秒短暂停顿，HUD 显示“接怪窗口”，Canvas 给每个裂隙怪绘制接怪环，路线压测验证首波不会突然贴脸。
- [ ] 下一轮继续实机观察首局 30 秒：结合真实操作截图/录像检查黄框倒计时、接怪窗口和首波敌人是否读起来舒服，必要时微调裂隙位置、敌人血量和超频目标，让 S 级开场靠打法而不是靠运气。

# 2026-07-08 自动推进补记

- [x] 增加首波先手截击奖励：快递裂隙落地后的接怪窗口内首次被武器/修复脉冲命中时，会真实发放 +1 bug点数和 +2 生命，写入跑局状态与存档，HUD 显示“命中触发先手截击”，Canvas 给命中的裂隙怪标出“先手截击”，并纳入开场裂隙与路线可读性探针。
- [x] 将先手截击接入开场评级与结算复盘：先手命中会点亮 `first-strike` 评分来源，S 级复盘显示“首异常/裂隙/先手/超频/路线”的得分构成，路线压测验证 HUD、档案最佳和结算卡均能读到该技巧点。
- [x] 将开场评级来源前移到战斗 HUD：首异常、裂隙、先手、超频、路线五个短芯片实时显示完成/点亮/错过状态，移动端自动换行，并由路线可读性与评级探针验证首局 30 秒内可扫读。
- [x] 增加 S 级开场即时补给：首局 30 秒评分首次达到 S 时只触发一次 `+2 bug点数 / +6 生命`，HUD 和结算复盘显示补给已入账，路线压测验证真实数值收益和存档状态。
- [ ] 下一轮继续做首局 30 秒实机观察：重点看黄框预告、落点、接怪窗口、先手截击和 S 级评分是否形成“看得到、打得到、想再试”的闭环；必要时继续调首波血量、落点、命中文字和推荐重开节奏。

# 2026-07-09 自动推进补记

- [x] 将 S 级开场升级为长期档案徽章：首次在首局 30 秒拿到 S 会永久点亮“S级开场”档案、额外发放 `+3 校准碎片`，开始界面和结算复盘都显示徽章归档状态，并由开场评级探针验证碎片、档案字段和 UI 文案。
- [x] 将 S 徽章接到专属局外成长节点：新增“首席夜巡印记”，首次 S 徽章给的 3 碎片刚好可购买 Lv.1；节点会让第一章开局 `+1 bug点数` 并让快递裂隙提前 8%，开始界面碎片栏提示推荐购买，节点按钮高亮，自动探针验证锁定/购买/数值/DOM 推荐态。
- [x] 将“首席夜巡印记”接入本地/Steam 成就闭环：购买该局外节点会解锁 `ACH_S_RANK_OPENER / 首席夜巡`，Steam 成就映射和成就文档同步更新，自动探针验证购买动作能真实解锁且不污染本地成就存储。
- [x] 增加“首席再巡挑战”：购买首席夜巡印记后，开始界面、局外成长摘要和战斗 HUD 会提示下一把 30 秒内再打出 S；完成后真实奖励 `+1 校准碎片`、记录完成次数/最佳分，并在结算复盘显示“首席再巡”卡，自动探针验证奖励、UI 和存档兼容。
- [x] 增加本地开场 Top 5 排行榜：每局开场评级会按分数和用时写入档案榜，开始界面显示“开场榜首”，结算复盘显示本局名次、用时和得分来源；首席再巡探针验证挑战完成后入榜 #1、UI 可读和存档迁移。
- [x] 增加开场 S 连胜挑战：连续打出 S 级开场会记录当前/最佳连胜，买下首席夜巡印记后每 `3` 连 S 额外奖励 `+2 校准碎片`；开始界面、局外摘要和结算复盘显示连胜目标，自动探针验证 x2 -> x3 的奖励、UI 和存档字段。
- [x] 将开场 S 连胜接入本地/Steam 成就：首席夜巡印记生效后连续 3 次 S 级开场会解锁 `ACH_OPENING_S_STREAK / 三连S开场`，Steam 草案、成就文档和首席挑战探针同步验证且还原本地成就存储。
- [x] 将开场榜扩展为推荐流派专属榜：每次开场评级会记录所用推荐流派，总榜保留 Top 5，每个流派单独保留 Top 3；开始页、推荐流派卡、结算统计和复盘卡都会显示流派榜首，首席挑战探针验证“键盘弹幕流”入榜。
- [x] 增加推荐流派首榜奖励：每个推荐流派首次以 S 级登上自己的 #1 会一次性获得 `+1 校准碎片`，档案记录 0/3-3/3 进度；开始页、流派卡、结算统计和复盘卡都显示奖励状态，首席挑战探针验证“键盘弹幕流”奖励已领。
- [x] 增加三流派全首榜成就：三套推荐流派都以 S 级开场登上各自 #1 后解锁 `ACH_ALL_BUILD_FIRSTS / 三流派首榜`，Steam 草案、成就文档和新路线探针同步验证 2/3 -> 3/3 的解锁链。
- [x] 把“开场榜首 + 流派榜首 + 首榜奖励 + S连胜 + 三流派首榜成就”做成商店截图里的榜单卖点：新增 `storeShot=leaderboard` 捕获路由，第 1 张 Steam 截图改为开场总榜、三套流派首榜、首榜奖励全领、S 连胜和成就计数同屏的局外成长证明图，并重新导出 PNG/contact sheet。
- [x] 提升榜单截图一眼可读性：开始页在三套推荐流派都拿到 #1 时改用“三套 #1 全S · 断点/键盘/修正”的短摘要，商店截图 manifest 增加“三套 #1 全S”复核项，并重新捕获第 1 张 PNG/contact sheet。
- [x] 把榜单闭环做成独立商店宣传图：新增 `leaderboard-meta-promo.png`，用“30秒开场榜 / 三套 #1 全S / S连胜 x6 / 首榜奖励 3/3 / 成就 11/11”压缩说明局外重开动力，并接入 `store-page.json` 复核资产、导出脚本和商店页校验。
- [ ] 下一轮继续观察榜单传播力：如果宣传图仍不够抓眼，就把榜单闭环做成短动态预告帧序列或 Steam 公告头图版式。
